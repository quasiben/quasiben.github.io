[project]
name = "quasiben-github-io"
version = "0.1.0"
description = "MkDocs source for quasiben.github.io"
requires-python = ">=3.14"
dependencies = [
    "mkdocs-glightbox>=0.5.2",
    "mkdocs-material",
]

[tool.uv]
package = false
